## Backend (FastAPI + ML)

- `konkor_data.ipynb`: model training notebook
- `model.pkl`: trained ML model exported from notebook
- `main.py`: FastAPI application serving predictions
- `konkur_results.csv`: dataset used for training